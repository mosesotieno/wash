---
title: "WASH Study Factors"
author: "Moses Otieno"
date: "`r format(Sys.time(), '%d %B, %Y')`"
output: 
  word_document: 
    reference_docx: "../reports/template.docx"
knit: (function(inputFile, encoding) {
    rmarkdown::render(inputFile, encoding = encoding, 
    output_dir = "../reports/")
  })
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE, message = FALSE, ft.align = "left", warning = FALSE)
options(gtsummary.tbl_summary.percent_fun = 
          function(x) sprintf(x * 100, fmt='%#.1f'))
```

```{r}
library(tidyverse)
library(janitor)
library(gtsummary)
library(flextable)
library(dmngt)
```

```{r}
wash <- read_rds("../data/wash_main_clean.rds")

wash_labels <- labels_keep(wash)

# for (x in names(wash_labels)){
#   var_label <- wash_labels[x]
#   wash_labels[x] <- paste0(var_label, " [", x, "]")
# }


wash <- wash |> 
  mutate(across(c(wqa01, wqa02, wqa03), as.factor))
  

wash <- labels_restore(wash, wash_labels)

wash_consented <- wash |> filter(consent == "Yes")

```





```{r}
gentable <- function(data = wash, variables) {
  

t1 <- data %>% 
  remove_empty(which = c("cols")) |> 
  select(any_of(variables)) %>% 
  tbl_summary( type = list(
               where(is.factor) ~ "categorical",
               where(is.numeric) ~ "continuous"),
               missing = "no",
               digits = list(all_categorical() ~ c(0, 1)),
               #statistic = list(where(is.Date) ~ "{date_iq_range}"),
               
               ) %>% 
  bold_labels() 

 


t1 <-  t1 |> 
  modify_header(label = "**Variable**") %>% 
  modify_footnote(everything() ~ NA) |> 
  as_flex_table() %>% 
  font(fontname = "Calibri", part = "all") %>%
  autofit() %>%
  padding(padding = 1, part = "all") %>% 
  fontsize(size = 10)
  
  

t1

}


# Assuming 'wash' contains the variable wqa04 (Total hardness, mg/L as CaCO3)
draw_plot <- function(variable, title_graph, ytitle){
  
  plot1 <- ggplot(wash_consented, aes(x = "", y = .data[[variable]])) +
  geom_boxplot(
    width = 0.6,
    fill = "grey90",
    colour = "black",
    outlier.shape = 1,
    outlier.size = 1.2
  ) +
  labs(
    title = title_graph,
    y = ytitle,
    x = NULL
  ) +
  theme_classic(base_size = 11) +
  theme(
    plot.title = element_text(size = 12, face = "bold", hjust = 0.5),
    plot.subtitle = element_text(size = 10, hjust = 0.5),
    axis.title.y = element_text(size = 11, face = "bold"),
    axis.text = element_text(size = 10, colour = "black"),
    axis.line = element_line(colour = "black", linewidth = 0.5),
    axis.ticks = element_line(colour = "black", linewidth = 0.5)
  ) +
  coord_cartesian(clip = "off") 
  
  plot1
  
}

draw_histogram <- function(data, variable, ptitle, xlabel = NULL,
                           bins = 30, log_transform = FALSE) {
  
  p <- ggplot(data, aes(x = .data[[variable]]))
  
  if(log_transform) {
    p <- p + geom_histogram(
      bins = bins,
      fill = "grey70",
      colour = "black",
      linewidth = 0.3
    ) +
      scale_x_log10(
        name = ifelse(is.null(xlabel), variable, xlabel),
        breaks = scales::log_breaks(n = 5)
      )
  } else {
    p <- p + geom_histogram(
      bins = bins,
      fill = "grey70",
      colour = "black",
      linewidth = 0.3
    ) +
      labs(x = ifelse(is.null(xlabel), variable, xlabel))
  }
  
  p + labs(y = "Frequency", title = ptitle) +
    theme_classic(base_size = 11) +
    theme(
      plot.title = element_text(size = 12, face = "bold", hjust = 0.5),
      axis.title.y = element_text(size = 11, face = "bold"),
      axis.title.x = element_text(size = 11, face = "bold"),
      axis.text = element_text(size = 10, colour = "black"),
      axis.line = element_line(colour = "black", linewidth = 0.5),
      axis.ticks = element_line(colour = "black", linewidth = 0.5)
    )
}
```



# Introduction

# Socio-Demographic Characteristics


```{r}

demos <- c("age", "age_cat", "marital_status", "education_level","hhsize", "hhold_income","sd08" )



gentable(wash_consented, demos)

```




# WASH Factors


```{r}


wash_vars <- c("wsp01", "wsp03", "wsp04", "wsp07", "wsp08", "wsp09", "wsp10", "wsp11",
               "wqa01", "wqa02", "wqa03", "sfp01","shares_latrine","num_sharing_latrine","sfp03","sfp04","sfp05","sfp06","sfp07","pad_category","sfp09","sfp10","sfp11", "hp01","hp02","hp03","hp04","hp05","hp06","hp07","hp08","ph_group")

gentable(wash_consented, wash_vars)
```


## Sexual Reproductive Health Hygiene


```{r}

sex_vars <- c("wsr01", "wsr02", "wsr03", "wsr03a", "wsr03b", "wsr03c", "wsr03d", "wsr08")

gentable(wash_consented, sex_vars)
```






# Plots

##  Total Hardness



```{r}

draw_plot("wqa04", "Distribution of Total Hardness of Water Samples", "Total hardness (mg/L as CaCO3)")
  # prevent outlier clipping if needed
```

## Total Alkalinity


```{r}
draw_plot("wqa05", "Distribution of Total Alkalinity of Water Samples", "Total Alkalinity (mg/L as CaCO3)")

```


## Carbonate


```{r}
draw_plot("wqa06", "Distribution of Carbonate in Water Samples", "Carbonate (mg/L)")


```


## Ecoli


```{r}
#draw_plot("e_coli", "Distribution of coliforms in Water Samples", "Coliforms * 100")
draw_histogram(wash_consented, "e_coli", 
               ptitle = "Distribution of Total Ecoli in Water Samples",
               xlabel = "Ecoli",
               log_transform = FALSE)

```




## Coliforms


```{r}

draw_histogram(wash_consented, "total_coliforms", 
               ptitle = "Distribution of Total Coliforms in Water Samples",
               xlabel = "Coliforms",
               log_transform = FALSE)
```


